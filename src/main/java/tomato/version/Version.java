package tomato.version;
/**
 * Tomato source baseline imported into the combined RealmShark build.
 * The application's artifact version is generated separately in realmshark.version.
 */
public class Version {
    public static final String VERSION = "v1.9.2";

    // Correcting release metadata does not change the extracted asset format.
    public static final String ASSET_CACHE_VERSION = "v1.9.1";
}

