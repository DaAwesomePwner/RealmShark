package tomato.gui.route;

/** Workspaces that can be opened by a typed {@link Route}. */
public enum Destination {
    RUNS, INSPECT, TIMELINE, RESOURCES, LOOT, STATISTICS,
    ENCOUNTER, NOTIFICATIONS, ALERT_DRAFT, BRIDGE_REVIEW, LOGGING, MY_INFO, CHARACTERS, QUESTS
}
